# firewall-allowlist.conf
# Domains and IP ranges allowed through the container firewall.
# One entry per line. Lines starting with # are comments. Empty lines ignored.
# @github is special: fetches IP ranges from api.github.com/meta

@github
api.anthropic.com
registry.npmjs.org
# sentry.io REMOVED (round 7): accepts arbitrary POST data — exfiltration channel.
# DISABLE_ERROR_REPORTING=1 is set in entrypoint.sh as defense-in-depth.
# statsig.anthropic.com REMOVED (round 9): accepts arbitrary POST data — exfiltration channel.
# statsig.com REMOVED (round 9): same as above.
# marketplace.visualstudio.com REMOVED (round 9): Azure CDN shared IPs — domain fronting risk.
# vscode.blob.core.windows.net REMOVED (round 9): Azure CDN shared IPs — domain fronting risk.
# update.code.visualstudio.com REMOVED (round 9): Azure CDN shared IPs — domain fronting risk.
api.todoist.com
