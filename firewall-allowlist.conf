# firewall-allowlist.conf
# Domains and IP ranges allowed through the container firewall.
# One entry per line. Lines starting with # are comments. Empty lines ignored.
# @github is special: fetches IP ranges from api.github.com/meta

@github
api.anthropic.com
registry.npmjs.org
# NOTE: sentry.io accepts arbitrary POST data — potential exfiltration channel.
# Claude Code's native binary has error reporting stubbed out (no-op), but this
# entry is kept for compatibility with Anthropic's documented network requirements.
# Consider removing if not needed: set DISABLE_ERROR_REPORTING=1 to be safe.
sentry.io
statsig.anthropic.com
statsig.com
marketplace.visualstudio.com
vscode.blob.core.windows.net
update.code.visualstudio.com
api.todoist.com
